@model HotelManagementSystem.Dto.RequestModel.CreateBooking

@{
    Layout = "_Layout";
    ViewData["Title"] = "Create Booking";
}

<h2>Create Booking</h2>

<form asp-controller="Booking" asp-action="CreateBooking">
    <div asp-validation-summary="ModelOnly" class="text-danger"></div>
@* 
    <div class="col-md-6" style="margin-top: 20px; font-size: large; font-weight: medium; color:dark;">
        <label asp-for="RoomId"></label>
        <select asp-for="RoomId" class="form-control" asp-items="@(ViewData["SelectRoom"])" required>
            <option value="">Select Room</option>mt       </select>
    </div> *@

    <div class="col-md-6" style="margin-top: 20px; font-size: large; font-weight: medium; color:dark;">
        <label asp-for="RoomId"></label>
        <select asp-for="RoomId" class="form-control" asp-items="@(ViewData["SelectRoom"] as IEnumerable<SelectListItem>)" required>
            <option value="">Select Room</option>
        </select>
    </div>


    <div class="form-group">
        <label asp-for="TotalCost"></label>
        <input asp-for="TotalCost" class="form-control" />
        <span asp-validation-for="TotalCost" class="text-danger"></span>
    </div>
    <div>
        <button type="submit" value="Create" class="btn btn-primary">Create</button>
    </div>
</form>



@* @model HotelManagementSystem.Dto.RequestModel.CreateBooking

@{
    Layout = "_Layout";
    ViewData["Title"] = "Create Booking";
    
}

<h2>Create Booking</h2>

<form asp-controller="Booking" asp-action="CreateBooking">
    <div asp-validation-summary="ModelOnly" class="text-danger"></div>
   

    <div class="col-md-6" style="margin-top: 20px; font-size: large; font-weight: medium; color:dark">
        <i><label class="control-label"></label> </i>
        <label asp-for="RoomId"></label>

        <select asp-for="RoomId" class="form-control" asp-items="ViewBag.SelectRoom" required>
            <option value=><i>Select Rooms </i></option>
        </select>
    </div>

    <div class="form-group">
        <label asp-for="TotalCost"></label>
        <input asp-for="TotalCost" class="form-control" />
        <span asp-validation-for="TotalCost" class="text-danger"></span>
    </div>
    <div>
        <button type="submit" value="Create" class="btn btn-primary"> Create  </button>
    </div>
</form>
 *@